package net.onefivefour.android.bitpot.model

/**
 * Used to define on which edge a View should have round edges.
 * For now only used in [net.onefivefour.android.bitpot.extensions] -> ViewExtensionsKt.setGradient
 */
enum class RoundEdgePosition {
    TOP, BOTTOM
}